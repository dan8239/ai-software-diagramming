---
description: Refine L4 (Code) class diagrams per container
---

Refine the L4 class-level diagrams.

1. Read `build/raw/manifest.json` for the package list.
2. For each package, read `build/raw/classes/classes_<package>.mmd`.
3. Read package source under `build/raw/source/` to identify the 1–3 most
   architecturally important classes per container (entrypoints, public API,
   base classes — not data holders or test helpers).
4. Write a trimmed Mermaid `classDiagram` to `out/views/L4_<package>.mmd`
   showing only those classes, their public methods, and inheritance/
   composition edges.
5. Report a one-line summary per container ("Kept 3 of 47 classes: …").

Do NOT push to Lucid yet — wait for user confirmation.
