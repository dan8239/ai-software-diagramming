---
description: Refine L1 (System Context) view in workspace.dsl
---

Refine the L1 system context view.

1. Personas: grep the source for auth/permission/role markers and propose
   personas beyond the seeded "User" (e.g. "Operator", "Admin", "API
   Consumer"). Confirm with the user.
2. External systems: grep for outbound integrations:
   - HTTP clients: `requests.`, `httpx.`, `urllib`, `aiohttp`
   - DB drivers: `psycopg`, `sqlalchemy`, `pymongo`, `redis`
   - Queues/streams: `kafka`, `pika`, `boto3.client('sqs'...)`, `celery`
   - Cloud SDKs: `boto3`, `google.cloud`, `azure.`
   - Auth: `oauth`, `jwt`, IdP-specific clients
3. Add each as a `softwareSystem` outside our system in `out/workspace.dsl`,
   with relationships (`our_system -> ext "verb-phrase using protocol"`).
4. Re-render the L1 view. This is the "first slide" diagram — should fit on a
   single screen with ≤10 boxes.
